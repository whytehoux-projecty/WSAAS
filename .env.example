# AURUM VAULT - Environment Variables Template
# Copy this file to .env and fill in actual values

# ============================================
# DEPLOYMENT CONFIGURATION
# ============================================
NODE_ENV=production

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_PASSWORD=change-this-secure-password
DATABASE_URL=postgresql://postgres:change-this-secure-password@postgres:5432/aurumvault

# ============================================
# REDIS CONFIGURATION
# ============================================
REDIS_URL=redis://redis:6379

# ============================================
# JWT SECRETS (GENERATE NEW ONES FOR PRODUCTION!)
# Use: openssl rand -base64 32
# ============================================
JWT_SECRET=your-jwt-secret-min-32-chars-CHANGE-THIS
JWT_REFRESH_SECRET=your-refresh-secret-min-32-chars-CHANGE-THIS
ADMIN_JWT_SECRET=your-admin-secret-min-32-chars-CHANGE-THIS
SESSION_SECRET=your-session-secret-min-32-chars-CHANGE-THIS

# ============================================
# CORS ORIGINS (Base origins for development)
# ============================================
CORS_ORIGINS=http://localhost:3002,http://localhost:4000,http://localhost:3003

# ============================================
# NGROK TUNNEL URLS (Updated after starting ngrok)
# These will be dynamic URLs from ngrok tunnels
# ============================================
NGROK_BACKEND_URL=https://your-backend-tunnel.ngrok.io
NGROK_ADMIN_URL=https://your-admin-tunnel.ngrok.io
NGROK_PORTAL_URL=https://your-portal-tunnel.ngrok.io

# ============================================
# NETLIFY DEPLOYMENT URL
# ============================================
CORPORATE_URL=https://aurumvault.netlify.app

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@aurumvault.com

# ============================================
# LOGGING CONFIGURATION
# ============================================
LOG_LEVEL=info

# ============================================
# FEATURE FLAGS (Optional)
# ============================================
ENABLE_SWAGGER=true
ENABLE_RATE_LIMITING=true

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Generate secure secrets:
#    openssl rand -base64 32
# 3. Update DB_PASSWORD with a strong password
# 4. Start Docker services: docker-compose up -d
# 5. Start ngrok tunnels (see scripts/start-ngrok.sh)
# 6. Update NGROK_* URLs with actual tunnel URLs
# 7. Restart Docker services to apply new URLs: docker-compose restart
# 8. Update Netlify environment variables with ngrok URLs
