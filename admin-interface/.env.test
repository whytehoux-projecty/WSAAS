# Test Environment Variables
NODE_ENV=test

# Test Database
TEST_DATABASE_URL=postgresql://test:test@localhost:5432/novabank_admin_test

# JWT Configuration (Test values)
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Bcrypt Configuration
BCRYPT_ROUNDS=4

# Server Configuration
PORT=3001
HOST=localhost

# Cookie Configuration
COOKIE_SECRET=test-cookie-secret-for-testing-only

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Rate Limiting (Relaxed for testing)
RATE_LIMIT_MAX=1000
RATE_LIMIT_WINDOW_MS=60000

# File Upload (Test values)
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads/test

# Admin Session
ADMIN_SESSION_TIMEOUT=3600000

# Security Headers (Disabled for testing)
SECURITY_HEADERS_ENABLED=false

# Logging
LOG_LEVEL=error

# External Services (Mock values for testing)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=testpassword
SMTP_FROM=noreply@novabank.test

# AWS S3 (Mock values)
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=test-bucket

# Twilio (Mock values)
TWILIO_ACCOUNT_SID=test-account-sid
TWILIO_AUTH_TOKEN=test-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Monitoring (Disabled for testing)
MONITORING_ENABLED=false
HEALTH_CHECK_ENABLED=false